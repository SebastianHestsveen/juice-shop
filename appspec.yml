version: 0.0
os: linux
files:
  - source: /
    destination : /
permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
hooks:
  BeforeInstall:
  - location: scripts/install.sh
    runas: root
  ApplicationStart:
    - location: scripts/start.sh
      runas: root